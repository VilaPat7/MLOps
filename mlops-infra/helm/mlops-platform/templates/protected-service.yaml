{{ if .Values.protected.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: cifar10-inference
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: cifar10-inference
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
{{ end }}
